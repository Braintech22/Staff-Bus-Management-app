import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const apiKey = '<YOUR_GOOGLE_API_KEY>';

ReactDOM.render( < App apiKey = { apiKey }
        />, document.getElementById('root'));